/// <reference types="vite/client" />

// Injected by vite.config.ts at config-evaluation time. See vite.config.ts
// `define` block for how the value is resolved (env > git > 'unknown').
declare const __PI_REMOTE_GIT_SHA__: string;
